<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:helpers="clr-namespace:LoreCompanion.Views.Helpers"
    xmlns:viewModels="clr-namespace:LoreCompanion.ViewModels"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="TextBlockStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  Style.FocusVisual.ListBoxItem  -->
    <Style x:Key="Style.FocusVisual.ListBoxItem">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle
                        Margin="0,1,0,1"
                        SnapsToDevicePixels="true"
                        Stroke="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"
                        StrokeDashArray="1 2"
                        StrokeThickness="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.ListBoxItem  -->
    <Style TargetType="{x:Type ListBoxItem}" x:Key="Style.ListBoxItem">
        <Setter Property="Background" Value="{StaticResource Brush.Background.Panel}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="{Binding Path=HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="VerticalContentAlignment" Value="{Binding Path=VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Height" Value="75" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="5"
                        Name="ItemBorder"
                        Padding="{TemplateBinding Padding}"
                        SnapsToDevicePixels="true">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter Property="BorderBrush" TargetName="ItemBorder" Value="{StaticResource Brush.Border.Selected}" />
                            <Setter Property="Background" TargetName="ItemBorder" Value="{StaticResource Brush.Background.Selected}" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Primary.Selected}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="BorderBrush" TargetName="ItemBorder" Value="{StaticResource Brush.Border.MouseOver}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.ListBox  -->
    <Style TargetType="{x:Type ListBox}" x:Key="Style.ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Primary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBox}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsGrouping" Value="true" />
                                <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.ListBoxItem.Navigation  -->
    <Style
        BasedOn="{StaticResource Style.ListBoxItem}"
        TargetType="{x:Type ListBoxItem}"
        x:Key="Style.ListBoxItem.Navigation">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border
                        Background="Transparent"
                        Padding="12,8"
                        x:Name="BackgroundBorder">
                        <ContentPresenter />
                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="BackgroundBorder" Value="{StaticResource Brush.Background.MouseOver.Navigation}" />
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" TargetName="BackgroundBorder" Value="{StaticResource Brush.Background.Selected.Navigation}" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Primary.Selected}" />
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>

                            <Setter Property="Background" TargetName="BackgroundBorder" Value="{StaticResource Brush.Background.MouseOver.Navigation}" />
                        </MultiTrigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" TargetName="BackgroundBorder" Value="Transparent" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  ItemTemplate.ListBox.Navigation  -->
    <DataTemplate DataType="{x:Type viewModels:SectionScreen}" x:Key="ItemTemplate.ListBox.Navigation">
        <StackPanel Orientation="Horizontal">

            <!--  <Image  -->
            <!--  Height="16"  -->
            <!--  Margin="0,0,8,0"  -->
            <!--  Source="{Binding Icon}"  -->
            <!--  Width="16" />  -->

            <TextBlock
                Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}"
                Text="{Binding DisplayName}"
                VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

    <!--  GroupStyle.ListBox.Navigation  -->
    <GroupStyle x:Key="GroupStyle.ListBox.Navigation">
        <GroupStyle.HeaderTemplate>
            <DataTemplate DataType="{x:Type CollectionViewGroup}">
                <TextBlock
                    Margin="8,12,8,4"
                    Style="{StaticResource Style.TextBlock.Heading.Navigation}"
                    Text="{Binding Name}" />
            </DataTemplate>
        </GroupStyle.HeaderTemplate>
    </GroupStyle>

    <!--  Style.ListBox.Navigation  -->
    <Style
        BasedOn="{StaticResource Style.ListBox}"
        TargetType="{x:Type ListBox}"
        x:Key="Style.ListBox.Navigation">
        <Setter Property="Background" Value="{StaticResource Brush.Background.Panel}" />
        <Setter Property="BorderThickness" Value="0,0,0.75,0" />
        <Setter Property="MinWidth" Value="200" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel IsItemsHost="True" VirtualizationMode="Recycling" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="helpers:GroupStyleHelper.GroupStyle" Value="{StaticResource GroupStyle.ListBox.Navigation}" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource Style.ListBoxItem.Navigation}" />
        <Setter Property="ItemTemplate" Value="{StaticResource ItemTemplate.ListBox.Navigation}" />
    </Style>

    <!--  Style.ListBoxItem.Implicit  -->
    <Style BasedOn="{StaticResource Style.ListBoxItem}" TargetType="{x:Type ListBoxItem}" />

    <!--  Style.ListBox.Implicit  -->
    <Style BasedOn="{StaticResource Style.ListBox}" TargetType="{x:Type ListBox}" />

</ResourceDictionary>