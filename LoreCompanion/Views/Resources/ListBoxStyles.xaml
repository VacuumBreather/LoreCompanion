<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cm="http://caliburnmicro.com"
    xmlns:converters="clr-namespace:LoreCompanion.Views.Converters"
    xmlns:helpers="clr-namespace:LoreCompanion.Views.Helpers"
    xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:viewModels="clr-namespace:LoreCompanion.ViewModels">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="TextBoxStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  Style.ListBoxItem  -->
    <Style x:Key="Style.ListBoxItem" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Background" Value="{StaticResource Brush.Background.Panel}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Border.CornerRadius" Value="{StaticResource CornerRadius.Controls}" />
        <Setter Property="HorizontalContentAlignment" Value="{Binding Path=HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="VerticalContentAlignment" Value="{Binding Path=VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="Padding" Value="10,5" />
        <Setter Property="Height" Value="75" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Border
                        Name="ItemBorder"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding Border.CornerRadius}"
                        SnapsToDevicePixels="true">
                        <DockPanel>
                            <Button
                                Margin="5"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                cm:Message.Attach="DeleteAsync($dataContext)"
                                DockPanel.Dock="Right"
                                Style="{StaticResource Style.Button.Delete}"
                                Visibility="{x:Static helpers:AdminHelper.AdminModeVisibility}" />
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="true">
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Selected}" />
                <Setter Property="Background" Value="{StaticResource Brush.Background.Selected}" />
                <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Primary.Selected}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.MouseOver}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  Style.ListBox  -->
    <Style x:Key="Style.ListBox" TargetType="{x:Type ListBox}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Primary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="helpers:ListBoxHelper.ScrollToSelectedItem" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBox}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ScrollViewer Padding="{TemplateBinding Padding}" Focusable="false">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsGrouping" Value="true" />
                                <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.ListBoxItem.Navigation  -->
    <Style
        x:Key="Style.ListBoxItem.Navigation"
        BasedOn="{StaticResource Style.ListBoxItem}"
        TargetType="{x:Type ListBoxItem}">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border
                        x:Name="BackgroundBorder"
                        Padding="12,8"
                        Background="Transparent">
                        <ContentPresenter />
                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BackgroundBorder" Property="Background" Value="{StaticResource Brush.Background.MouseOver.Navigation}" />
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="BackgroundBorder" Property="Background" Value="{StaticResource Brush.Background.Selected.Navigation}" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.Foreground.Primary.Selected}" />
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>

                            <Setter TargetName="BackgroundBorder" Property="Background" Value="{StaticResource Brush.Background.MouseOver.Navigation}" />
                        </MultiTrigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="BackgroundBorder" Property="Background" Value="Transparent" />
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Converter.NavigationIcon  -->
    <converters:NavigationIconConverter x:Key="Converter.NavigationIcon">
        <helpers:TypeIconMapping Icon="{x:Static icon:PackIconMaterialDesignKind.Home}" Type="{x:Type viewModels:DashboardViewModel}" />
        <helpers:TypeIconMapping Icon="{x:Static icon:PackIconMaterialDesignKind.Person}" Type="{x:Type viewModels:CharactersViewModel}" />
        <helpers:TypeIconMapping Icon="{x:Static icon:PackIconRPGAwesomeKind.Broadsword}" Type="{x:Type viewModels:ItemsViewModel}" />
        <helpers:TypeIconMapping Icon="{x:Static icon:PackIconMaterialDesignKind.VideoLibrary}" Type="{x:Type viewModels:EpisodesViewModel}" />
    </converters:NavigationIconConverter>

    <!--  ItemTemplate.ListBox.Navigation  -->
    <DataTemplate x:Key="ItemTemplate.ListBox.Navigation" DataType="{x:Type viewModels:SectionScreen}">
        <DockPanel>
            <icon:PackIconControl
                VerticalAlignment="Center"
                DockPanel.Dock="Left"
                Foreground="{StaticResource Brush.Foreground.Primary}"
                Kind="{Binding Converter={StaticResource Converter.NavigationIcon}}" />
            <TextBlock
                Margin="10,0"
                VerticalAlignment="Center"
                Text="{Binding DisplayName}" />
        </DockPanel>
    </DataTemplate>

    <!--  GroupStyle.ListBox.Navigation  -->
    <GroupStyle x:Key="GroupStyle.ListBox.Navigation">
        <GroupStyle.HeaderTemplate>
            <DataTemplate DataType="{x:Type CollectionViewGroup}">
                <TextBlock
                    Margin="8,12,8,4"
                    Style="{StaticResource Style.TextBlock.Heading.Navigation}"
                    Text="{Binding Name}" />
            </DataTemplate>
        </GroupStyle.HeaderTemplate>
    </GroupStyle>

    <!--  Style.ListBox.Navigation  -->
    <Style
        x:Key="Style.ListBox.Navigation"
        BasedOn="{StaticResource Style.ListBox}"
        TargetType="{x:Type ListBox}">
        <Setter Property="MinWidth" Value="200" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel IsItemsHost="True" VirtualizationMode="Recycling" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="helpers:GroupStyleHelper.GroupStyle" Value="{StaticResource GroupStyle.ListBox.Navigation}" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource Style.ListBoxItem.Navigation}" />
        <Setter Property="ItemTemplate" Value="{StaticResource ItemTemplate.ListBox.Navigation}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBox}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsGrouping" Value="true" />
                                <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.ListBoxItem.Implicit  -->
    <Style BasedOn="{StaticResource Style.ListBoxItem}" TargetType="{x:Type ListBoxItem}" />

    <!--  Style.ListBox.Implicit  -->
    <Style BasedOn="{StaticResource Style.ListBox}" TargetType="{x:Type ListBox}" />

</ResourceDictionary>