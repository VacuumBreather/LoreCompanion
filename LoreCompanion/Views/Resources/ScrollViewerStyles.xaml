<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="clr-namespace:LoreCompanion.Views.Helpers"
    xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="ButtonStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <LinearGradientBrush x:Key="HorizontalScrollBarPageButtonNormal" StartPoint="0, 0" EndPoint="0, 1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Offset="0" Color="#D8E9FF" />
            <GradientStop Offset="0.05" Color="#D8E9FF" />
            <GradientStop Offset="0.06" Color="#E4EFFF" />
            <GradientStop Offset="0.94" Color="#F9FBFF" />
            <GradientStop Offset="0.95" Color="#D8E9FF" />
            <GradientStop Offset="1" Color="#D8E9FF" />
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="HorizontalScrollBarPageButtonPressed" StartPoint="0, 0" EndPoint="0, 1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Offset="0" Color="#B4C8E0" />
            <GradientStop Offset="0.05" Color="#B4C8E0" />
            <GradientStop Offset="0.06" Color="#C6DAF2" />
            <GradientStop Offset="0.94" Color="#F2F7FF" />
            <GradientStop Offset="0.95" Color="#B4C8E0" />
            <GradientStop Offset="1" Color="#B4C8E0" />
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="VerticalScrollBarPageButtonNormal" StartPoint="0, 0" EndPoint="1, 0">
        <LinearGradientBrush.GradientStops>
            <GradientStop Offset="0" Color="#D8E9FF" />
            <GradientStop Offset="0.05" Color="#D8E9FF" />
            <GradientStop Offset="0.06" Color="#E4EFFF" />
            <GradientStop Offset="0.94" Color="#F9FBFF" />
            <GradientStop Offset="0.95" Color="#D8E9FF" />
            <GradientStop Offset="1" Color="#D8E9FF" />
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="VerticalScrollBarPageButtonPressed" StartPoint="0, 0" EndPoint="1, 0">
        <LinearGradientBrush.GradientStops>
            <GradientStop Offset="0" Color="#B4C8E0" />
            <GradientStop Offset="0.05" Color="#B4C8E0" />
            <GradientStop Offset="0.06" Color="#C6DAF2" />
            <GradientStop Offset="0.94" Color="#F2F7FF" />
            <GradientStop Offset="0.95" Color="#B4C8E0" />
            <GradientStop Offset="1" Color="#B4C8E0" />
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <!--  Style.RepeatButton.ScrollBar  -->
    <Style x:Key="Style.RepeatButton.ScrollBar" TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Padding" Value="2" />
        <Setter Property="Background" Value="{StaticResource Brush.Background.Button}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="helpers:ScrollBarHelper.IconKind" Value="ArrowDropUp" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Viewbox>
                            <icon:PackIconMaterialDesign Kind="{TemplateBinding helpers:ScrollBarHelper.IconKind}" />
                        </Viewbox>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.RepeatButton.ScrollBar.Page  -->
    <Style
        x:Key="Style.RepeatButton.ScrollBar.Page"
        BasedOn="{StaticResource Style.RepeatButton.ScrollBar}"
        TargetType="{x:Type RepeatButton}">
        <Setter Property="Padding" Value="5" />
        <Setter Property="Background" Value="{StaticResource Brush.Background.Panel}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.Thumb.ScrollBar  -->
    <Style x:Key="Style.Thumb.ScrollBar" TargetType="{x:Type Thumb}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border
                        Background="{StaticResource Brush.Background.Button}"
                        BorderBrush="{StaticResource Brush.Border}"
                        BorderThickness="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.ScrollBar  -->
    <Style x:Key="Style.ScrollBar" TargetType="{x:Type ScrollBar}">
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
        <Setter Property="Background" Value="{StaticResource Brush.Background.Panel}" />
        <Setter Property="Width" Value="12" />
        <Setter Property="MinWidth" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                        <Grid.RowDefinitions>
                            <RowDefinition MaxHeight="12" />
                            <RowDefinition Height="0.00001*" />
                            <RowDefinition MaxHeight="12" />
                        </Grid.RowDefinitions>
                        <RepeatButton Command="{x:Static ScrollBar.LineUpCommand}" Style="{StaticResource Style.RepeatButton.ScrollBar}" />
                        <Track
                            Name="PART_Track"
                            Grid.Row="1"
                            IsDirectionReversed="true">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource Style.RepeatButton.ScrollBar.Page}" />
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource Style.RepeatButton.ScrollBar.Page}" />
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource Style.Thumb.ScrollBar}" />
                            </Track.Thumb>
                        </Track>
                        <RepeatButton
                            Grid.Row="2"
                            helpers:ScrollBarHelper.IconKind="ArrowDropDown"
                            Command="{x:Static ScrollBar.LineDownCommand}"
                            Style="{StaticResource Style.RepeatButton.ScrollBar}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="Height" Value="12" />
                <Setter Property="MinHeight" Value="12" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition MaxWidth="12" />
                                    <ColumnDefinition Width="0.00001*" />
                                    <ColumnDefinition MaxWidth="12" />
                                </Grid.ColumnDefinitions>
                                <RepeatButton
                                    helpers:ScrollBarHelper.IconKind="ArrowLeft"
                                    Command="{x:Static ScrollBar.LineLeftCommand}"
                                    Style="{StaticResource Style.RepeatButton.ScrollBar}" />

                                <Track Name="PART_Track" Grid.Column="1">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Style="{StaticResource Style.RepeatButton.ScrollBar.Page}" />
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Style="{StaticResource Style.RepeatButton.ScrollBar.Page}" />
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource Style.Thumb.ScrollBar}" />
                                    </Track.Thumb>
                                </Track>

                                <RepeatButton
                                    Grid.Column="2"
                                    helpers:ScrollBarHelper.IconKind="ArrowRight"
                                    Command="{x:Static ScrollBar.LineRightCommand}"
                                    Style="{StaticResource Style.RepeatButton.ScrollBar}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  Style.ScrollBar.Implicit  -->
    <Style BasedOn="{StaticResource Style.ScrollBar}" TargetType="{x:Type ScrollBar}" />

    <!--  Style.ScrollViewer  -->
    <Style x:Key="Style.ScrollViewer" TargetType="{x:Type ScrollViewer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <ScrollContentPresenter
                            x:Name="PART_ScrollContentPresenter"
                            Grid.Row="0"
                            Grid.Column="0"
                            Margin="{TemplateBinding Padding}"
                            CanContentScroll="{TemplateBinding CanContentScroll}"
                            CanHorizontallyScroll="False"
                            CanVerticallyScroll="False"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}" />
                        <ScrollBar
                            x:Name="PART_VerticalScrollBar"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="3,0,0,0"
                            AutomationProperties.AutomationId="VerticalScrollBar"
                            Cursor="Arrow"
                            Maximum="{TemplateBinding ScrollableHeight}"
                            Minimum="0"
                            Style="{StaticResource Style.ScrollBar}"
                            ViewportSize="{TemplateBinding ViewportHeight}"
                            Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                            Value="{TemplateBinding VerticalOffset}" />
                        <ScrollBar
                            x:Name="PART_HorizontalScrollBar"
                            Grid.Row="1"
                            Grid.Column="0"
                            Margin="0,3,0,0"
                            AutomationProperties.AutomationId="HorizontalScrollBar"
                            Cursor="Arrow"
                            Maximum="{TemplateBinding ScrollableWidth}"
                            Minimum="0"
                            Orientation="Horizontal"
                            Style="{StaticResource Style.ScrollBar}"
                            ViewportSize="{TemplateBinding ViewportWidth}"
                            Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                            Value="{TemplateBinding HorizontalOffset}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.ScrollViewer.Implicit  -->
    <Style BasedOn="{StaticResource Style.ScrollViewer}" TargetType="{x:Type ScrollViewer}" />

</ResourceDictionary>