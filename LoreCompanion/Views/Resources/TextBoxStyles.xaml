<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="clr-namespace:LoreCompanion.Views.Helpers"
    xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="ScrollViewerStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  Style.TextBox  -->
    <Style x:Key="Style.TextBox" TargetType="{x:Type TextBox}">
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Border.CornerRadius" Value="{StaticResource CornerRadius.Controls}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Foreground}" />
        <Setter Property="Background" Value="{StaticResource Brush.Background.Panel}" />
        <Setter Property="Padding" Value="5,10" />
        <Setter Property="FontFamily" Value="{StaticResource FontFamily.Default}" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="AllowDrop" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="SelectionBrush" Value="{StaticResource Brush.Background.TextBox.Selection}" />
        <Setter Property="AcceptsReturn" Value="True" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBoxBase}">
                    <Border
                        Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding Border.CornerRadius}">
                        <ScrollViewer x:Name="PART_ContentHost" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.TextBox.Search  -->
    <Style
        x:Key="Style.TextBox.Search"
        BasedOn="{StaticResource Style.TextBox}"
        TargetType="{x:Type TextBox}">
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBoxBase}">
                    <Border
                        Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding Border.CornerRadius}">
                        <DockPanel>
                            <icon:PackIconMaterialDesign
                                Margin="10,5"
                                VerticalAlignment="Center"
                                DockPanel.Dock="Left"
                                Foreground="{StaticResource Brush.Foreground.Dark}"
                                Kind="Search" />
                            <Button
                                Margin="10,5"
                                VerticalAlignment="Center"
                                helpers:TextBoxHelper.IsClearButton="True"
                                DockPanel.Dock="Right"
                                Style="{StaticResource Style.Button.TextBox.Clear}" />
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                VerticalAlignment="Center"
                                VerticalContentAlignment="Center" />
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.TextBox.Header  -->
    <Style
        x:Key="Style.TextBox.Header"
        BasedOn="{StaticResource Style.TextBox}"
        TargetType="{x:Type TextBox}">
        <Setter Property="Margin" Value="0,0,0,20" />
        <Setter Property="MaxHeight" Value="300" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBoxBase}">
                    <DockPanel>
                        <TextBlock
                            Margin="0,0,0,10"
                            VerticalAlignment="Top"
                            DockPanel.Dock="Top"
                            Style="{StaticResource Style.TextBlock.Heading}"
                            Text="{Binding (helpers:TextBoxHelper.Header), RelativeSource={RelativeSource TemplatedParent}}" />
                        <Border
                            Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}">
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                VerticalAlignment="Center"
                                VerticalContentAlignment="Center" />
                        </Border>
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Style.TextBox.Implicit  -->
    <Style BasedOn="{StaticResource Style.TextBox}" TargetType="{x:Type TextBox}" />
</ResourceDictionary>