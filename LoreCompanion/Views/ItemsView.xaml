<UserControl
    x:Class="LoreCompanion.Views.ItemsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:LoreCompanion.ViewModels"
    xmlns:views="clr-namespace:LoreCompanion.Views"
    d:DataContext="{d:DesignInstance viewModels:ItemsViewModel,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">

    <views:MasterDetailControl
        EditDetailTemplate="{StaticResource DataTemplate.Item.Detail.Edit}"
        EditMode="{Binding EditMode, Mode=OneWay}"
        ItemTemplate="{StaticResource DataTemplate.Item.Master}"
        ReadOnlyDetailTemplate="{StaticResource DataTemplate.Item.Detail.ReadOnly}" />

</UserControl>