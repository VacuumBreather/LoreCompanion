<UserControl
    x:Class="LoreCompanion.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:behaviors="clr-namespace:LoreCompanion.Views.Behaviors"
    xmlns:controls="clr-namespace:LoreCompanion.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:effects="clr-namespace:LoreCompanion.Views.Effects"
    xmlns:helpers="clr-namespace:LoreCompanion.Views.Helpers"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:LoreCompanion.Models"
    xmlns:viewModels="clr-namespace:LoreCompanion.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:DashboardViewModel,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <UserControl.Background>
        <VisualBrush Stretch="UniformToFill">
            <VisualBrush.Visual>
                <Image Source="/LoreCompanion;component/Views/Resources/Images/Background.png">
                    <Image.Effect>
                        <effects:TintShaderEffect TintColor="{StaticResource Color.Background.5}" />
                    </Image.Effect>
                </Image>
            </VisualBrush.Visual>
        </VisualBrush>
    </UserControl.Background>
    <UserControl.Resources>

        <!--  Style.ItemContainerStyle.ItemsControl  -->
        <Style x:Key="Style.ItemContainerStyle.ItemsControl" TargetType="{x:Type ContentPresenter}">
            <Setter Property="Width" Value="260" />
            <Setter Property="Height" Value="180" />
            <Setter Property="Margin" Value="10" />
        </Style>

        <!--  DataTemplateSelector.Dashboard  -->
        <helpers:EntityTemplateSelector x:Key="DataTemplateSelector.Dashboard">
            <helpers:EntityTemplateSelector.LocationTemplate>
                <DataTemplate DataType="{x:Type models:Location}">
                    <Border
                        Padding="5,15"
                        Background="{StaticResource Brush.Background.Panel}"
                        BorderBrush="{StaticResource Brush.Border}"
                        BorderThickness="1">
                        <DockPanel>
                            <DockPanel
                                Margin="5,0"
                                HorizontalAlignment="Center"
                                DockPanel.Dock="Top">
                                <iconPacks:PackIconRPGAwesome
                                    Width="20"
                                    Height="20"
                                    VerticalAlignment="Center"
                                    DockPanel.Dock="Left"
                                    Foreground="{StaticResource Brush.Foreground.Primary}"
                                    Kind="Tower" />
                                <Viewbox
                                    MaxHeight="24"
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center">
                                    <TextBlock Style="{StaticResource Style.TextBlock.Heading}" Text="{Binding Name}" />
                                </Viewbox>
                            </DockPanel>
                            <Image
                                Margin="0,20,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Top"
                                RenderOptions.BitmapScalingMode="HighQuality"
                                Source="{StaticResource Image.Placeholder}"
                                Stretch="Uniform">
                                <b:Interaction.Behaviors>
                                    <behaviors:LoadCachedImageBehavior Url="{Binding ImageUrl}" />
                                </b:Interaction.Behaviors>
                            </Image>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </helpers:EntityTemplateSelector.LocationTemplate>
            <helpers:EntityTemplateSelector.ItemTemplate>
                <DataTemplate DataType="{x:Type models:Item}">
                    <Border
                        Padding="5,15"
                        Background="{StaticResource Brush.Background.Panel}"
                        BorderBrush="{StaticResource Brush.Border}"
                        BorderThickness="1">
                        <DockPanel>
                            <DockPanel
                                Margin="5,0"
                                HorizontalAlignment="Center"
                                DockPanel.Dock="Top">
                                <iconPacks:PackIconRPGAwesome
                                    Width="20"
                                    Height="20"
                                    VerticalAlignment="Center"
                                    DockPanel.Dock="Left"
                                    Foreground="{StaticResource Brush.Foreground.Primary}"
                                    Kind="{Binding Type, Converter={StaticResource Converter.ItemTypeToIcon}}" />
                                <Viewbox
                                    MaxHeight="24"
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center">
                                    <TextBlock Style="{StaticResource Style.TextBlock.Heading}" Text="{Binding Name}" />
                                </Viewbox>
                            </DockPanel>
                            <Image
                                Margin="0,20,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Top"
                                RenderOptions.BitmapScalingMode="HighQuality"
                                Source="{StaticResource Image.Placeholder}"
                                Stretch="Uniform">
                                <b:Interaction.Behaviors>
                                    <behaviors:LoadCachedImageBehavior Url="{Binding ImageUrl}" />
                                </b:Interaction.Behaviors>
                            </Image>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </helpers:EntityTemplateSelector.ItemTemplate>
            <helpers:EntityTemplateSelector.DialogTemplate>
                <DataTemplate DataType="{x:Type models:Dialog}">
                    <Border
                        Padding="5,15"
                        Background="{StaticResource Brush.Background.Panel}"
                        BorderBrush="{StaticResource Brush.Border}"
                        BorderThickness="1">
                        <DockPanel>
                            <DockPanel
                                Margin="5,0"
                                HorizontalAlignment="Center"
                                DockPanel.Dock="Top">
                                <iconPacks:PackIconMaterialDesign
                                    Width="20"
                                    Height="20"
                                    VerticalAlignment="Center"
                                    DockPanel.Dock="Left"
                                    Foreground="{StaticResource Brush.Foreground.Primary}"
                                    Kind="Chat" />
                                <Viewbox
                                    MaxHeight="24"
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center">
                                    <TextBlock Style="{StaticResource Style.TextBlock.Heading}" Text="{Binding Character.Name, FallbackValue='Unknown'}" />
                                </Viewbox>
                            </DockPanel>
                            <TextBlock
                                MaxWidth="200"
                                Margin="0,20,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Top"
                                DockPanel.Dock="Left"
                                Style="{StaticResource Style.TextBlock.Body.Quote.Small}"
                                Text="{Binding Content}"
                                TextTrimming="WordEllipsis"
                                TextWrapping="Wrap" />
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </helpers:EntityTemplateSelector.DialogTemplate>
            <helpers:EntityTemplateSelector.CharacterTemplate>
                <DataTemplate DataType="{x:Type models:Character}">
                    <Border
                        Padding="5,15"
                        Background="{StaticResource Brush.Background.Panel}"
                        BorderBrush="{StaticResource Brush.Border}"
                        BorderThickness="1">
                        <DockPanel>
                            <DockPanel
                                Margin="5,0"
                                HorizontalAlignment="Center"
                                DockPanel.Dock="Top">
                                <iconPacks:PackIconMaterialDesign
                                    Width="20"
                                    Height="20"
                                    VerticalAlignment="Center"
                                    DockPanel.Dock="Left"
                                    Foreground="{StaticResource Brush.Foreground.Primary}"
                                    Kind="Person" />
                                <Viewbox
                                    MaxHeight="24"
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center">
                                    <TextBlock Style="{StaticResource Style.TextBlock.Heading}" Text="{Binding Name}" />
                                </Viewbox>
                            </DockPanel>
                            <Image
                                Margin="0,20,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Top"
                                RenderOptions.BitmapScalingMode="HighQuality"
                                Source="{StaticResource Image.Placeholder}"
                                Stretch="Uniform">
                                <b:Interaction.Behaviors>
                                    <behaviors:LoadCachedImageBehavior Url="{Binding ImageUrl}" />
                                </b:Interaction.Behaviors>
                            </Image>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </helpers:EntityTemplateSelector.CharacterTemplate>
        </helpers:EntityTemplateSelector>
    </UserControl.Resources>

    <controls:BusyDecorator IsBusy="{Binding IsBusy, Mode=OneWay}">
        <DockPanel Margin="10,20">
            <TextBlock
                HorizontalAlignment="Center"
                DockPanel.Dock="Top"
                Style="{StaticResource Style.TextBlock.Heading.Large}"
                Text="Latest Discoveries" />
            <ScrollViewer
                Margin="0,10,0,0"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto">
                <ItemsControl
                    HorizontalAlignment="Center"
                    ItemContainerStyle="{StaticResource Style.ItemContainerStyle.ItemsControl}"
                    ItemTemplateSelector="{StaticResource DataTemplateSelector.Dashboard}"
                    ItemsSource="{Binding ItemsView}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </controls:BusyDecorator>

</UserControl>